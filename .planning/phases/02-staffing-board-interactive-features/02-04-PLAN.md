---
phase: 02-staffing-board-interactive-features
plan: 04
type: execute
wave: 3
depends_on: ["02-01", "02-02", "02-03"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "All Phase 2 features work together on a live staffing board"
    - "Post-it notes coexist with drag-and-drop without interference"
    - "No regressions to existing staffing board functionality"
  artifacts: []
  key_links: []
---

<objective>
Verify all Phase 2 interactive features work correctly together: post-it notes, drag handles, drag-to-home, drag-to-copy, and visual feedback. Confirm no regressions to existing functionality.

Purpose: Human verification ensures the visual/interactive features meet the user's expectations from the CONTEXT.md discussion. DnD interactions and post-it styling require human eyes to confirm.

Output: Human-verified confirmation that all Phase 2 features work as specified.
</objective>

<execution_context>
@./.opencode/workflows/execute-plan.md
@./.opencode/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-staffing-board-interactive-features/02-CONTEXT.md
@.planning/phases/02-staffing-board-interactive-features/02-01-SUMMARY.md
@.planning/phases/02-staffing-board-interactive-features/02-02-SUMMARY.md
@.planning/phases/02-staffing-board-interactive-features/02-03-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Verify all Phase 2 interactive features</name>
  <what-built>
    Complete staffing board interactive features:
    1. Post-it notes with 3 colors (yellow/red/blue), create/edit/delete, text truncation
    2. Visible drag handles on all assignment cards (always visible, not hover)
    3. Assignment drag overlay showing employee name + "Moving assignment" badge
    4. Drag assignment to Home → goingHome status + moveOutDate today
    5. Drag assignment to different project → copy (stays in both)
    6. Home drop zone with red visual feedback during assignment drags
    7. Two side-by-side buttons: "Add employee" + "Add post-it"
  </what-built>
  <how-to-verify>
    Start dev server: `pnpm dev` from monorepo root. Navigate to Staffing Board.

    **Post-it Notes (CONTEXT.md decisions):**
    1. Verify each project column has TWO side-by-side buttons at bottom: "Add employee" and "Add post-it"
    2. Click "Add post-it" — a yellow post-it card should appear at bottom of that project column
    3. Click the post-it card — popover opens with textarea (min-rows=3, max-rows=7) and 3 color buttons (yellow/red/blue)
    4. Type a long paragraph of text, close popover — card should truncate text at 3 lines
    5. Click the post-it again, select "red" color — both popover AND card background should turn red
    6. Click X button on a post-it corner — card should disappear immediately
    7. Create multiple post-its on same project — they should be ordered by creation date (newest last)
    8. Refresh the page — all notes should persist

    **Drag & Drop (CONTEXT.md decisions):**
    9. Verify every assignment card has a visible grip icon (6 dots) on the left side — always visible, NOT hover-only
    10. Grab an assignment card by grip handle, start dragging — original card stays at ~80% opacity, overlay follows cursor
    11. Test existing drag: Drag employee from left panel to project → assigned with yellow status
    12. Test drag to Home: Drag assignment card to employee list panel. Verify: (a) drop zone appears when drag starts, (b) red highlight on hover, (c) on drop: status changes to goingHome (pink), no popup, no toast
    13. Test copy to project: Drag assignment to different project. Verify: (a) blue highlight on target column, (b) on drop: new assignment appears in target + original stays in source, (c) same status preserved, (d) no popup, no toast
    14. Test same-column drop: Drag assignment card within same project → nothing happens (silent cancel)
    15. Test duplicate: Try dragging to project where employee already assigned → orange "Already exists" notification
    16. Test button clicks: Click edit/status/travel buttons on assignment card → should work without triggering drag
    17. Cancel drag: Start dragging, press Escape → card returns to normal, no state changes

  </how-to-verify>
  <resume-signal>Type "approved" if all 17 checks pass, or describe any issues found.</resume-signal>
</task>

</tasks>

<verification>
Human has verified all interactive features against CONTEXT.md decisions.
</verification>

<success_criteria>

- All 17 verification checks pass
- No regressions to existing staffing board functionality
- Post-it notes and drag-and-drop features work together without interference
  </success_criteria>

<output>
After completion, create `.planning/phases/02-staffing-board-interactive-features/02-04-SUMMARY.md`
</output>
