---
phase: 04-mobile-infrastructure
plan: 03
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - apps/mobile/src/**/*.tsx
  - apps/mobile/src/**/*.ts
autonomous: false

must_haves:
  truths:
    - "TypeScript compiler reports no errors in the mobile app"
    - "No stale or unused code remains in the mobile codebase"
    - "All existing modules render correctly on simulators"
    - "App launches and navigates between features without crashes"
  artifacts:
    - path: "apps/mobile/src/app"
      provides: "Clean Expo Router routes with no broken references"
    - path: "apps/mobile/src/views"
      provides: "Feature view screens with no stale imports or dead code"
  key_links:
    - from: "apps/mobile/src/app/_layout.tsx"
      to: "apps/mobile/src/context/AuthContext.tsx"
      via: "AuthProvider wrapping Stack"
      pattern: "AuthProvider"
    - from: "apps/mobile/src/app/(application)/app/_layout.tsx"
      to: "apps/mobile/src/views"
      via: "Drawer routes rendering view components"
      pattern: "Drawer"
---

<objective>
Clean up the mobile codebase by fixing TypeScript errors, removing stale code, and verifying all existing modules render correctly on simulators.

Purpose: The mobile app was migrated from React Navigation to Expo Router. The old /screens and /navigation directories have been removed, but there may be stale imports, unused dependencies, broken component references, or TypeScript errors remaining. This plan ensures the codebase is clean and all features work before beta deployment.

Output: A clean codebase with zero TypeScript errors, no dead code, and all modules verified working on simulators.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-mobile-infrastructure/04-RESEARCH.md
@.planning/phases/04-mobile-infrastructure/04-01-SUMMARY.md
@apps/mobile/package.json
@apps/mobile/tsconfig.json
@apps/mobile/src/app/_layout.tsx
@apps/mobile/src/app/(application)/app/_layout.tsx
@apps/mobile/src/app/(application)/app/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run TypeScript check and fix all errors</name>
  <files>apps/mobile/src/**/*.tsx, apps/mobile/src/**/*.ts</files>
  <action>
1. Run TypeScript compiler in no-emit mode from the mobile app directory:
   ```
   cd apps/mobile && npx tsc --noEmit
   ```

2. Fix ALL TypeScript errors found. Common issues to expect:
   - Missing type imports after dependency updates
   - Stale references to removed /screens or /navigation directories
   - Type mismatches from updated expo-router or React Native types
   - Unused imports or variables

3. Check for stale code patterns:
   - Search for any imports from "#/screens/" or "#/navigation/" (these dirs no longer exist)
   - Search for any `createNativeStackNavigator` usage (old React Navigation pattern)
   - Search for any `navigation.navigate` calls (should all be `router.push` now)
   - Check if `apps/mobile/src/app/(application)/app/form-fields/` directory contains dead/example code that should be removed

4. Look for duplicate shared components:
   - `apps/mobile/src/shared/EmptyContainer.tsx` AND `apps/mobile/src/views/shared/EmptyContainer.tsx` -- consolidate or remove the duplicate
   - `apps/mobile/src/shared/InfoRow.tsx` AND `apps/mobile/src/views/shared/InfoRow.tsx` -- same issue

5. Remove the stale build artifact at the root of mobile app:
   - `apps/mobile/build-1764784568021.tar.gz` -- this is a leftover build file that should not be in the repo

6. After fixes, re-run `npx tsc --noEmit` to confirm zero errors.

IMPORTANT: Do NOT refactor working code for "cleanliness" -- only fix actual errors and remove genuinely dead code. Do NOT change navigation patterns, component structure, or business logic. The goal is cleanup, not rewrite.
  </action>
  <verify>
- `cd apps/mobile && npx tsc --noEmit` exits with code 0 (no errors)
- `grep -r "screens/" apps/mobile/src/ --include="*.tsx" --include="*.ts"` returns no results (no stale imports)
- `grep -r "createNativeStackNavigator" apps/mobile/src/ --include="*.tsx" --include="*.ts"` returns no results
- Build artifact removed: `ls apps/mobile/build-*.tar.gz` returns no results
  </verify>
  <done>
- TypeScript compiler reports zero errors
- No stale imports referencing old /screens or /navigation directories
- No old React Navigation patterns remaining
- Duplicate shared components consolidated
- Stale build artifact removed
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Updated Expo SDK 54 dependencies, configured monorepo hoisting, created EAS Build profiles, cleaned up codebase with zero TypeScript errors, and removed stale code.
  </what-built>
  <how-to-verify>
1. Start the mobile dev server from repo root:
   ```
   cd apps/mobile && npx expo start -c
   ```

2. Open iOS Simulator (press 'i' in terminal) and verify:
   - App launches without crash
   - Login screen appears (or dashboard if already authenticated)
   - Navigate via drawer menu to each feature:
     a. Company Directory -- list loads, tapping employee opens detail modal
     b. Employees -- list renders with cards
     c. Inventory Items -- list renders with item cards
     d. Projects -- list renders with project cards
     e. Tickets -- list renders
     f. My Profile -- profile screen loads
     g. My Certifications -- list renders

3. (Optional) Open Android Emulator (press 'a' in terminal) and verify same features render.

4. Confirm no red error screens, no "Unable to resolve module" errors, no blank screens.

Expected: All existing modules render their lists correctly. Detail/modal views open when tapping items. Drawer navigation works between all features.
  </how-to-verify>
  <resume-signal>Type "approved" if all modules render correctly, or describe which modules have issues.</resume-signal>
</task>

</tasks>

<verification>
1. `cd apps/mobile && npx tsc --noEmit` passes with zero errors
2. App launches on iOS simulator without crashes
3. All drawer menu features (company directory, employees, inventory, projects, tickets, profile, certifications) render correctly
4. Navigation between features works via drawer
5. Detail/modal views open correctly
</verification>

<success_criteria>
- Zero TypeScript errors in mobile app
- No stale code or dead imports remaining
- All existing modules (inventory, employees, tickets, projects, company directory, certifications, profile) render correctly on simulators
- App navigates between all features without crashes
- Development environment validated with updated dependencies
</success_criteria>

<output>
After completion, create `.planning/phases/04-mobile-infrastructure/04-03-SUMMARY.md`
</output>
